function soma(n1=0, n2=0){ // "=0", pois se não chamar a variável ela fica com o valor zero
    return n1 + n2 // retorna a soma de n1 com n2
}
console.log(soma(3,5))// chamada completa
console.log(soma(2))// chamada incompleta que passou a valer zero